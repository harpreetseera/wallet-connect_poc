name: wallet_connect_poc
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.6.0
  qr_code_scanner: ^1.0.1
  app_settings: ^5.0.0
  walletconnect_flutter_v2: ^2.1.4
  mobx: ^2.2.0
  mobx_codegen: ^2.3.0
  flutter_mobx: ^2.0.6+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  
flutter:
  uses-material-design: true
